"""Sage workflow flag parser.

Parses --quality-locked and --autonomous flags from $ARGUMENTS for
/build and /architect slash commands. Returns a structured result
that the agent can trust unconditionally.
"""

__version__ = "1.0.0"
