# E16 — measured runs

Not yet run. Registered as E-REV-1 for the `review_loop: v2` flip
criteria (RR-28): pass N=3 majority required, alongside E17/E18 and the
calibration thresholds, before the default flips in its own commit
citing the numbers.
